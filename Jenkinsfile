node('shiv') {
        stage('provision-fyre-node') {
              //checkout scm
              def build_ok = true
              sh '''
              pwd
              if [[ ${build_ok} = true ]] ; then exit 1 ; fi
              
              '''
        }
        
        
        
      }
