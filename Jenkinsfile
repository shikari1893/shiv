node('shiv') {
        stage('provision-fyre-node') {
              //checkout scm
              def build_ok = true
              sh '''
              pwd
              pwd=$?
              if [[ ${pwd} = 1 ]] ; then exit 1 ; fi
              
              '''
        }
        
        
        
      }
