node('shiv') {
        stage('provision-fyre-node') {
              //checkout scm
              def build_ok = true
              sh '''
              pwd
              pwd=$?
              if [[ ${pwd} = 0 ]] ; then exit 0 ; fi
              
              '''
        }
        
        
        
      }
